{
  "name": "my-project-mobile-app",
  "version": "1.2.8",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build script\" && exit 0",
    "bootstrap": "pnpm i && bundle install",
    "fastlane": "bundle exec fastlane",
    "release": "echo \"No release script\" && exit 0",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx"
  },
  "dependencies": {
    "uuid": "9.0.0",
    "yargs": "17.7.1"
  },
  "devDependencies": {
    "@types/jest": "29.4.0"
  }
}
