{
  "name": "my-project-backend",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build script\" && exit 0",
    "release": "echo \"No release script specified\" && exit 0"
  }
}
